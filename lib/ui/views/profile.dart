import 'package:flutter/material.dart';
import 'package:myapp/ui/widgets/profile_header.dart';

class Profile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Stack(
      children: [
        ListView(
          children: [],
        ),
        ProfileHeader()
      ],
    );
  }
}
